function GolArmy(index, name, icon, cb, color, power, budget) {

    var that = this;

    that.index = index;
    that.name = name;
    that.icon = icon;
    that.cb = cb;
    that.color = color;
    that.power = power;
    that.budget = budget;
}